# CLAUDE.md

このファイルは、Claude Code にこのリポジトリで作業してもらうための指示書です。

## プロジェクト概要

このリポジトリは、証券会社CSV/スプレッドシートを Google Apps Script で読み込み、取引DBへ保存し、法人投資管理用の出力シートを生成する株管理ツールです。

主な対象証券会社:

- 野村證券
- 楽天証券

現在の大きな方針:

- 野村処理と楽天処理は段階的に分離する。
- ただし、平均取得単価・簿価・保有数量・損益などの計算コアは共通化する。
- 楽天を「野村形式に変換する」のではなく、楽天用DB/入力/出力/ロールバックを分け、必要な計算用レコードだけ共通モデルへ変換する。

## 作業前に必ず読むファイル

作業開始時は、まず以下を確認する。

- `AGENTS.md`
- `docs/current-status.md`
- `docs/TODO.md`
- 必要に応じて `docs/spec.md`
- 必要に応じて `docs/trade-rules.md`

Claude Codeでレビューや実装を始める前に、現在のPR・未マージPR・マージ済みPRの前提を `docs/current-status.md` で確認すること。

## Branch / Commit Rules

- 実装タスクでは、`develop` に直接コミットしない。
- 新しい実装・修正タスクでは、作業開始前に最新の `develop` から作業ブランチを作る。
- ブランチ名は `feature/<task-name>` を基本にする。
- ユーザーが作業ブランチ名を指定している場合は、そのブランチ上で作業する。
- 変更は作業ブランチにだけコミットする。
- PR作成前に、まだ追加のコード変更をせず、未コミット差分レビューを行う。
- 実装後は、以下を要約する。
  - 変更ファイル
  - 差分概要
  - 実行したテスト
  - 未確認事項
- `develop` への直接コミットは、ユーザーが明示した小さなドキュメント/ステータス更新などに限定する。

## Claude Codeの推奨役割

Claude Codeは、主に以下の用途で使う。

- Codexが作ったPRの別視点レビュー
- 大きめの設計変更の影響範囲調査
- GAS / V8 実行時エラー候補の発見
- 未定義関数・呼び出し不一致・テスト不足の確認
- ローカルでのgrep、差分確認、テスト実行支援

実装を行う場合でも、まずは「調査だけ」「レビューだけ」から始め、いきなり大きな変更をしない。

## 技術前提

- Google Apps Script / V8 を前提とする。
- Node.js専用APIやブラウザ専用APIをGASサーバー側コードへ追加しない。
- HtmlService側のクライアントJSとGASサーバー関数の呼び出し名を一致させる。
- clasp管理の場合でも、GAS側のファイル名・関数名との整合性を崩さない。
- 既存関数名の互換性を優先する。
- ヘッダー名は既存仕様と厳密一致を基本とする。
- DBスキーマは既存の `BASE_HEADERS` / `DB_HEADERS` / `RAKUTEN_DB_HEADERS` / `IMPORT_LOG_HEADERS` を確認してから変更する。

## 重要な設計方針

- 証券会社別CSVは、直接計算処理へ流さず、取込処理で検出・正規化してからDBへ保存する。
- 野村DBと楽天DBは分離する。
- 野村DBは従来の `DB_HEADERS` / `BASE_HEADERS` を基本とする。
- 楽天DBは `RAKUTEN_DB_HEADERS` を基本とする。
- 楽天DBから出力・計算処理へ渡す場合は、必要に応じて共通計算用レコードへ変換する。
- 楽天専用の入力処理・DB保存処理・出力処理・ロールバック処理は段階的に分ける。
- 計算コアは、野村/楽天で共通化する。

## DBルーティング方針

現在のUI選択と実際の追加先DBの関係:

- 野村入力 + `nomura_corp_a` → `nomura_corp_a`
- 野村入力 + `nomura_corp_b` → `nomura_corp_b`
- 野村入力 + `nomura_test` → `nomura_test`
- 楽天入力 + `nomura_corp_a` → `rakuten_corp_a`
- 楽天入力 + `nomura_corp_b` → `rakuten_corp_b`
- 楽天入力 + `nomura_test` → `rakuten_test`

楽天DBは `uiVisible: false` とし、取込用UIの直接選択肢には出さない。
ただし、DBリセット/ロールバック用UIでは楽天DBも選択できる。

過去のドキュメントや会話に `corp_a` / `corp_b` / `test` が出る場合があるが、現在の通常DBキーは `nomura_*` を基本とする。

## 楽天証券対応の重要前提

楽天処理は、野村形式へ無理に寄せるのではなく、楽天用処理として分離していく。

楽天配当金CSVについて:

- 楽天配当金CSVには、ユーザーが手動で以下の3カラムを追加する。
  - `レート`
  - `現地源泉税［円］`
  - `国内源泉税［円］`
- この3カラムは楽天配当金CSVだけの補完情報として扱う。
- 野村CSVや楽天株式CSVには要求しない。
- 未入力時のエラー/警告、ヘッダー表記ゆれ許容、DB保存方針は設計・テストで明確にする。

## レビュー観点

PRや未コミット差分をレビューするときは、特に以下を見る。

- GAS / V8で未定義関数や実行時エラーが起きないか
- `google.script.run` の呼び出し名とサーバー側関数名が一致しているか
- `RAKUTEN_DB_HEADERS` と実際の保存/読込処理が一致しているか
- 楽天DBと野村DBのヘッダー切り替えで既存DBを壊さないか
- 旧レイアウトの楽天DBを誤ってヘッダーだけ上書きしないか
- 楽天DBから共通計算用レコードへ戻す変換で情報欠落がないか
- `rowHash` / `importId` / `isActive` / `rolledBackAt` の扱いが壊れていないか
- リセット/ロールバックが対象DBだけに効くか
- 既存の野村DB処理を壊していないか
- テストが変更内容を本当に固定しているか
- PR本文に確認済み/未確認事項が正直に書かれているか

## テスト方針

- 既存テストを壊さない。
- 新しい取引区分・証券会社フォーマットを追加したら、検出・正規化・DB保存・読込・DBルーティングのテストを追加する。
- GAS上で実行できるテスト関数名を明記する。
- テスト用DBでは赤セルバリデーションを無視する設計を維持する。
- DB作成・Drive操作を含むテストは、実行ユーザーとDrive共有権限に注意する。
- GAS反映前は、必要に応じて `runSmokeTests` / `runAllTests` を確認する。

## Webアプリ / OAuth / Drive権限

`DriveApp.getFolderById()` や `moveTo(folder)` を使う処理では、Webアプリの実行ユーザーとDrive共有権限に注意する。

Webアプリを「アクセスしているユーザー」として実行する場合:

- 利用者本人にDBフォルダの編集権限が必要。
- 利用者本人がDrive OAuth権限を承認する必要がある。
- `appsscript.json` の `oauthScopes` に `https://www.googleapis.com/auth/drive` と `https://www.googleapis.com/auth/spreadsheets` が必要。
- manifestを変更した後は、Webアプリを新しいバージョンで再デプロイする。

現在のユーザー方針は「アクセスしているユーザー」として実行する運用。

## コーディング注意点

- GASで未対応の構文や外部依存を安易に追加しない。
- 既存の `text_()` / `toNumber_()` / `parseDate_()` / `normalizeCurrency_()` などのユーティリティを優先して使う。
- 新しいユーティリティを使う場合は、必ず定義が存在することを確認する。
- ヘッダー正規化は、空白、全角/半角カッコ、全角スラッシュなどを吸収するが、DB出力ヘッダー自体は既存仕様を維持する。
- 金額の丸めや簿価計算は既存仕様を壊さない。
- `-0` 表示や1円ズレ対策など、過去に修正した数値正規化方針を後退させない。

## PR/変更の出し方

- 大きな変更は小さなPRに分ける。
- 仕様変更、DBスキーマ変更、出力列変更は必ずドキュメントを更新する。
- 楽天対応の追加は、フォーマットごとに段階的に進める。
- 変更後に実行したテスト名と結果をPR本文へ書く。
- PR本文には、確認済み項目と未確認項目を分けて書く。
- マージ後に状態が変わった場合は `docs/current-status.md` を更新する。

## Claude Codeに依頼される典型タスク

### PRレビュー

```text
PR #XX をレビューしてください。
まだコード変更しないでください。

観点:
- GAS / V8 実行時エラー
- 未定義関数
- 野村/楽天DB分離方針との整合
- 楽天DBヘッダー切替による既存DB破壊リスク
- 楽天DB読み戻し変換の情報欠落
- テスト不足
- マージ前に直すべきブロッカー
```

### 調査だけ

```text
以下の目的で調査してください。
まだコード変更しないでください。

目的:
- <目的>

出力:
1. 結論
2. 根拠ファイル
3. 影響範囲
4. 最小修正案
5. 必要なテスト
```

### 最小実装

```text
以下の目的で最小差分だけ実装してください。
AGENTS.md と CLAUDE.md のルールに従ってください。

develop には直接コミットしないでください。
```
