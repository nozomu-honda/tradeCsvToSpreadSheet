# Codexショートカット運用メモ

このドキュメントは、Codexへ依頼するときに使うプロンプトテンプレートと、AutoHotkeyによるショートカット入力の使い方をまとめたものです。

リポジトリ内では、以下の場所に置く想定です。

```text
docs/codex-shortcuts.md
```

## 目的

Codexへの依頼を毎回長文で書かず、短いショートカット入力でテンプレートを呼び出せるようにする。

例:

```text
;cx03
```

と入力すると、PRレビュー用のテンプレートが展開される。

## 前提ファイル

プロンプト本体は、以下のファイルにまとめておく。

```text
docs/codex-prompts.md
```

AutoHotkey側は、各ショートカットから `docs/codex-prompts.md の Txx を使ってください` という短い依頼文を展開する。

## AutoHotkeyの使い方

### 1. AutoHotkey v2をインストールする

Windowsでは AutoHotkey v2 を使う。

### 2. ショートカット定義ファイルを起動する

以下のファイルをダブルクリックして起動する。

```text
codex-prompts-autohotkey-v2-instant.ahk
```

起動すると、Windows右下のタスクトレイに AutoHotkey のアイコンが出る。

### 3. 動作確認

メモ帳などで以下を入力する。

```text
;ahtest
```

これが以下に変わればOK。

```text
OK
```

### 4. Codex入力欄で使う

Codexの入力欄で以下のように入力する。

```text
;cx03
```

PRレビュー用テンプレートが展開される。

## PC起動時に自動で有効化する方法

`.ahk` ファイルを毎回ダブルクリックしたくない場合は、Windowsのスタートアップにショートカットを入れる。

手順:

1. `.ahk` ファイルを任意の場所に置く
2. `Win + R` を押す
3. 以下を入力してEnter

```text
shell:startup
```

4. 開いたフォルダに `.ahk` ファイルのショートカットを置く

次回PC起動時から自動でAutoHotkeyが起動する。

## ショートカット一覧

### 基本テンプレート

| 入力 | 対応テンプレート | 用途 |
|---|---|---|
| `;cx00` | T00 | 文脈確認。AGENTS/current-status/TODOを読ませて要約させる |
| `;cx01` | T01 | 調査だけ。まだコード変更させない |
| `;cx02` | T02 | 最小差分で実装 |
| `;cx03` | T03 | PRレビュー |
| `;cx04` | T04 | PR差分の危険箇所洗い出し |
| `;cx05` | T05 | 複数PRのマージ順整理 |
| `;cx06` | T06 | テスト追加だけ |
| `;cx07` | T07 | バグ修正 |
| `;cx08` | T08 | UIだけ修正 |
| `;cx09` | T09 | DBまわり修正 |
| `;cx10` | T10 | 楽天CSVフォーマット追加 |
| `;cx11` | T11 | GAS反映前チェック |
| `;cx12` | T12 | ドキュメントだけ更新 |
| `;cx13` | T13 | 既存コードの削除前確認 |
| `;cx14` | T14 | 変更を小さなPRに分割する相談 |
| `;cx15` | T15 | PR本文作成 |
| `;cx16` | T16 | マージ後の作業リスト作成 |
| `;cx17` | T17 | PR作成 |

### よく使う別名

| 入力 | 用途 |
|---|---|
| `;cxq` | 調査だけ |
| `;cxi` | 最小実装 |
| `;cxpr` | PRレビュー |
| `;cxbug` | バグ修正 |
| `;cxui` | UI修正 |
| `;cxdb` | DB修正 |
| `;cxgas` | GAS反映前チェック |
| `;cxdoc` | ドキュメントだけ更新 |
| `;cxmkpr` | PR作成 |

## おすすめ運用

### 1. まず調査だけさせる

最初から実装させず、まずは `;cx01` または `;cx03` を使う。

```text
;cx01
```

目的・対象ファイルを埋めて、まだコード変更しないようにする。

### 2. 方針が合っていたら最小実装

調査結果が妥当なら、次に `;cx02` を使う。

```text
;cx02
```

対象ファイルを絞って、最小差分で実装させる。

### 3. PRはまずレビューだけ

Codexが作ったPRは、いきなり追加修正させず、まず `;cx03` でレビューだけさせる。

```text
;cx03
```

### 4. GAS反映前は必ずチェック

GASへ反映する前は `;cx11` を使う。

```text
;cx11
```

確認する主な項目:

- `appsscript.json` の OAuth scope
- GAS / V8 構文
- `google.script.run` の呼び出し名
- DB_CONFIG の実値混入
- DriveAppの権限・実行ユーザー
- `runSmokeTests` / `runAllTests`

### 5. PR作成はT17を使う

作業ブランチの実装・テスト・コミットが終わったら、`T17` を使って draft PR を作成する。

```text
;cx17
```

または別名:

```text
;cxmkpr
```

PR作成後は、まだマージせず `;cx03` でPRレビューする。

## 注意点

- Codexアプリ本体にテンプレートボタンを追加する仕組みではない
- AutoHotkeyはPC上で文字入力を展開するだけ
- ChatGPTの入力欄に `;cx03` と打っても展開されない
- AutoHotkeyが起動していないとショートカットは効かない
- Codexアプリで効かない場合は、ブラウザ版Codexやメモ帳で確認する
- 管理者権限のアプリ上で効かない場合は、AutoHotkeyも管理者として実行する

## このプロジェクトでの基本方針

- Codexは実装担当
- ChatGPTは設計・整理・レビュー支援担当
- Codex使用量を節約するため、いきなり大きな依頼をしない
- 1タスク1目的にする
- 対象ファイルを明示する
- `AGENTS.md` / `docs/codex-handoff.md` / `docs/decisions.md` は原則触らせない
- ドキュメント更新は必要最小限にする
